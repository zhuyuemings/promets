<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wx.userinfo">
    <!-- 保存微信用户信息 -->
    <insert id="saveWxUserInfo" parameterType="ltd.jezhu.promets.dto.user.WxUserInfoDTO">
        insert into wx_user_info (
            openid, nick_name, avatar_url, gender, country, province, city, language, user_id
        ) values (
            #{openid}, #{nickName}, #{avatarUrl}, #{gender}, #{country}, #{province}, #{city}, #{language}, #{userId}
        )
    </insert>
</mapper>